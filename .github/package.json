{
  "name": "validator-runner-tests",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "vitest run --pool=forks --poolOptions.forks.singleFork"
  },
  "devDependencies": {
    "vitest": "^3.1.2"
  }
}